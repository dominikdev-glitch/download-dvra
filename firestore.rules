rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Admin chat messages collection
    match /_admin_chats/{messageId} {
      allow read, write: if true;
    }

    // Database explorer and generic collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
